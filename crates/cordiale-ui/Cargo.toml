[package]
name = "cordiale-ui"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
cordiale-core = { path = "../cordiale-core" }
slint = "1"

[build-dependencies]
slint-build = "1"
