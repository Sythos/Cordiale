[package]
name = "cordiale-ui"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
cordiale-core = { path = "../cordiale-core" }
serde_json = "1"
slint = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }

[build-dependencies]
slint-build = "1"
