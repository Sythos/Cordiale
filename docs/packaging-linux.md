# Linux packaging

Spazio riservato agli artefatti `.tar.gz` standalone e `.deb` per Linux x64 e
ARM64, con target Debian 13 Trixie e Ubuntu 26.04 LTS.

La compilazione e il packaging devono avvenire nel workflow GitHub Actions
`packages.yml`, mai nella macchina di sviluppo.
